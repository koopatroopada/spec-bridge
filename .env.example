# spec-bridge 环境变量
#
# MVP 不使用任何环境变量——所有数据存 localStorage，不调外部 API。
# 用户拿到导出的 Promptfoo YAML 后，用自己的 API key 在自己的终端跑。
#
# 以下变量为未来可能引入的占位（如果做"在 app 内预览 prompt 输出"功能时会需要）：

# OpenAI API key（仅在未来"app 内预览"功能启用时使用）
# OPENAI_API_KEY=sk-...

# Anthropic API key（仅在未来"app 内预览"功能启用时使用）
# ANTHROPIC_API_KEY=sk-ant-...

# Vercel 部署时，构建产物的 base URL（如自定义域名）
# NEXT_PUBLIC_SITE_URL=https://spec-bridge.example.com
